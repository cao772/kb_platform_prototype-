# Stage37 真实采集、解析与准入查询记录

执行基线：`7dc7fc3849d65d556a16e9c2d0a190bb2c34cc84` 加本轮修复。执行时间：2026-09-22。

本次为有边界的实网抽样，不代表已爬完 50 个站点：5 站并发，同域请求至少间隔 1 秒；每站最多 6 个成功页面、12 次尝试和 150 秒预算。标准站继续只采公开元数据，不下载版权全文。

## 运行摘要

|结果|站点数|
|---|---:|
|受限或未取到内容|18|
|仅动态页面外壳|6|
|跟进到子页面，待筛选业务相关性|18|
|仅入口页|8|

- `eCFR` 与 `Federal Register` 均返回 Request Access 限制页，已标为失败并排除内容入库。
- 德国 `ProdSG` 成功走通：入口、法规全文及条款、PDF、XML 压缩包、HTML/PDF/XML 解析、候选校核。
- 美国 DOE 成功取得洗碗机、制冷产品、电视相关页面及一份电视测试程序历史附件；空调、洗衣机详情本轮超时。
- 本轮解析 9 份产品相关样本，产生 39 条待人工校核的候选；候选未自动转为正式知识。

## 本地 OCR

平台的自动 OCR 在发现本机可用 PaddleOCR 与缓存模型时优先调用本地引擎；不可用时继续按原模型配置回退。密钥、本地环境路径、模型缓存和原始文件均不提交。

|文件|页码|策略|识别字符数|
|---|---:|---|---:|
|Permenperin No. 7 Tahun 2025-安全.pdf|1|local_ocr|1987|
|Permenperin No. 7 Tahun 2025-安全.pdf|40|local_ocr|603|
|Permenperin No. 7 Tahun 2025-安全.pdf|77|local_ocr|530|

## 产品准入接口实测

通过本地 HTTP 接口执行 24 次查询：6 个产品族 × 德国、美国、加拿大、新加坡。每项的产品分类和必填参数均匹配；所有返回均为 `insufficient_data`，原因是现有正式知识尚未覆盖这些产品/市场，新抓取候选仍待人工校核。

因此当前可展示产品分类与证据检索，但不能把这 24 项宣称为正式准入结论。DOE 电视历史文件也不能单独作为当前要求依据；现行 eCFR 在本轮请求中受访问限制，仍需以可访问的现行官方文本复核。

## 已验证修复

- 零页面采集标为失败；部分页面失败标为部分完成，并返回原因。
- HTML 优先提取 `main` 正文、排除脚本和导航噪声，并读取声明字符集。
- XML 使用文本节点顺序提取，避免父子节点造成重复正文。
- 本地 OCR、混合页 OCR 统计与失败页统计均写入逐页解析报告。
- 项目可研/申请资料归类为项目背景资料，原始法规归类为法规知识，避免背景资料混入法规限定检索。

详细的逐请求 HTTP、robots、错误、原始抓取件和接口响应是可再生运行产物，保留在本机 `data/live_validation_stage37/`，不提交到仓库以避免把原始下载材料和运行数据库带入版本控制。
